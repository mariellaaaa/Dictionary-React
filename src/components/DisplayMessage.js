import React from 'react'

const DisplayMessage = ({searchMessage}) => {
    return (
        <div className="search-message">
            {searchMessage}
        </div>
    )
}

export default DisplayMessage;